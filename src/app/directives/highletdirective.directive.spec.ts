import { HighletdirectiveDirective } from './highletdirective.directive';

describe('HighletdirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new HighletdirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
